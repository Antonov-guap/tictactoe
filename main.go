package main

func main() {
	// g := createGame()
	runStdout()
	// drawFyneWindow(&g)
	// TODO:
	// stdout + stdin (done)
	// fyne + buttons (done)
	// sdl+opengl
	// go-server + browser + vue.js
	// go-server + electron + vue.js
	// + network to all previous
	// telegram bot
	// slack bot
	// xcode
	// unity3d
}
